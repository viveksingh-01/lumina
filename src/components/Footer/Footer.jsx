import React from "react";

function Footer() {
  return (
    <footer className="my-2 p-2 text-center">
      Lumina uses Gemini under the hood and can make mistakes, so double-check
      it.
    </footer>
  );
}

export default Footer;
