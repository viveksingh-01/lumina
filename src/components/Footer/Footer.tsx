const Footer: React.FC = () => {
  return (
    <footer className="my-2 p-2 text-center text-sm text-[#999]">
      Lumina uses Gemini under the hood and can make mistakes, so double-check it.
    </footer>
  );
};

export default Footer;
